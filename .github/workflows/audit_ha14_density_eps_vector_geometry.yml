name: Audit Ha14 density EPS vector geometry
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/stationary/audit_ha14_density_eps_vector_geometry.py'
      - '.github/workflows/audit_ha14_density_eps_vector_geometry.yml'
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Audit Ha14 density EPS vector geometry
        run: python scripts/stationary/audit_ha14_density_eps_vector_geometry.py
      - name: Commit vector audit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add validation/stationary/ha14_density_eps_vector_geometry_v1.json
          if git diff --cached --quiet; then
            echo 'No changes'
          else
            git commit -m 'Audit Ha14 density EPS vector geometry'
            for attempt in 1 2 3; do
              git pull --rebase origin main
              if git push origin HEAD:main; then exit 0; fi
              sleep 2
            done
            exit 1
          fi
