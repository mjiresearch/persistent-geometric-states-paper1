name: Audit Ge04 native HI profile vector route
on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/stationary/audit_ge04_hi_profile_vector_route.py'
      - '.github/workflows/audit_ge04_hi_profile_vector_route.yml'
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/stationary/audit_ge04_hi_profile_vector_route.py
      - name: Commit Ge04 audit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add validation/stationary/ge04_hi_profile_vector_route_v1.json
          if git diff --cached --quiet; then echo 'No changes'; else
            git commit -m 'Audit Ge04 native HI profile vector route'
            for attempt in 1 2 3; do git pull --rebase origin main; if git push origin HEAD:main; then exit 0; fi; sleep 2; done
            exit 1
          fi
