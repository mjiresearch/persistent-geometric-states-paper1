name: Inspect SA87 HEROES Figure 29 vector PDF

on:
  push:
    paths:
      - 'scripts/stationary/inspect_sa87_heroes_fig29_pdf_vector.py'
      - '.github/workflows/inspect_sa87_heroes_fig29_pdf_vector.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install PDF parser
        run: python -m pip install --quiet pymupdf
      - name: Inspect Figure 29 PDF geometry
        run: python scripts/stationary/inspect_sa87_heroes_fig29_pdf_vector.py
      - name: Commit vector inventory race-safe
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add validation/stationary/sa87_heroes_fig29_pdf_vector_inventory_v1.json validation/stationary/sa87_heroes_fig29_pdf_vector_context_v1.txt
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Inspect HEROES Figure 29 native PDF vectors'
          for attempt in 1 2 3; do
            if git pull --rebase origin main && git push origin HEAD:main; then exit 0; fi
            git rebase --abort || true
            git fetch origin main
            git reset --hard origin/main
            python -m pip install --quiet pymupdf
            python scripts/stationary/inspect_sa87_heroes_fig29_pdf_vector.py
            git add validation/stationary/sa87_heroes_fig29_pdf_vector_inventory_v1.json validation/stationary/sa87_heroes_fig29_pdf_vector_context_v1.txt
            git commit -m 'Inspect HEROES Figure 29 native PDF vectors' || true
            sleep 2
          done
          exit 1
