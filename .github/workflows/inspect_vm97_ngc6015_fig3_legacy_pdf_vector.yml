name: Inspect VM97 NGC6015 Figure 3 legacy PDF vector

on:
  push:
    paths:
      - 'scripts/stationary/inspect_vm97_ngc6015_fig3_legacy_pdf_vector.py'
      - '.github/workflows/inspect_vm97_ngc6015_fig3_legacy_pdf_vector.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --quiet pymupdf
      - name: Inspect legacy PDF page geometry
        run: python scripts/stationary/inspect_vm97_ngc6015_fig3_legacy_pdf_vector.py
      - name: Commit PDF inventory race-safe
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add validation/stationary/vm97_ngc6015_fig3_legacy_pdf_vector_inventory_v1.json validation/stationary/vm97_ngc6015_fig3_legacy_pdf_vector_context_v1.txt
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Inspect VM97 NGC6015 Figure 3 legacy PDF vectors'
          for attempt in 1 2 3; do
            if git pull --rebase origin main && git push origin HEAD:main; then exit 0; fi
            git rebase --abort || true
            git fetch origin main
            git reset --hard origin/main
            python scripts/stationary/inspect_vm97_ngc6015_fig3_legacy_pdf_vector.py
            git add validation/stationary/vm97_ngc6015_fig3_legacy_pdf_vector_inventory_v1.json validation/stationary/vm97_ngc6015_fig3_legacy_pdf_vector_context_v1.txt
            git commit -m 'Inspect VM97 NGC6015 Figure 3 legacy PDF vectors' || true
            sleep 2
          done
          exit 1
