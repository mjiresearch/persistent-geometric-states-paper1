name: Audit Allaert 2015 NGC5907 helium convention
on:
  push:
    paths:
      - 'scripts/stationary/audit_allaert2015_ngc5907_helium_convention.py'
      - '.github/workflows/audit_allaert2015_ngc5907_helium_convention.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/stationary/audit_allaert2015_ngc5907_helium_convention.py
      - name: Commit audit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add validation/stationary/allaert2015_ngc5907_helium_convention_v1.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Audit NGC5907 helium convention'
          git pull --rebase origin main
          git push origin HEAD:main
