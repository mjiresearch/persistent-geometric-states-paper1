name: Audit Hoekstra 2001 Fig1 vector structure

on:
  push:
    branches: [main]
    paths:
      - 'scripts/stationary/audit_hoekstra2001_profs_vector_structure.py'
      - '.github/workflows/audit_hoekstra2001_profs_vector_structure.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Parse Fig1 EPS structure
        run: python scripts/stationary/audit_hoekstra2001_profs_vector_structure.py
      - name: Commit audit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add validation/stationary/hoekstra2001_profs_vector_structure_v1.json
          if git diff --cached --quiet; then
            echo 'No changes'
          else
            git commit -m 'Audit Hoekstra Fig1 vector structure'
            for attempt in 1 2 3; do
              git pull --rebase origin main
              if git push origin HEAD:main; then exit 0; fi
              sleep 2
            done
            exit 1
          fi
