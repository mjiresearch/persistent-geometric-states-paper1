name: Query OSC candidate Gaia epoch RVs
on:
  workflow_dispatch:
  push:
    paths:
      - 'data/persistence_history/hi4pi_grb_geometric_distance/cepheid_streaming_frozen/query_osc_candidate_gaia_epoch_rvs.py'
      - '.github/workflows/query-osc-candidate-gaia-epoch-rvs.yml'
permissions:
  contents: write
jobs:
  query:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install pandas requests
      - name: Query Gaia DR3 epoch-RV tables
        run: python data/persistence_history/hi4pi_grb_geometric_distance/cepheid_streaming_frozen/query_osc_candidate_gaia_epoch_rvs.py
      - name: Commit lookup products
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/persistence_history/hi4pi_grb_geometric_distance/cepheid_streaming_frozen/osc_rv_target_rank/gaia_epoch_rv_lookup
          if ! git diff --cached --quiet; then
            git commit -m 'Add Gaia epoch-RV availability for OSC targets'
            git push
          fi
